---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Free Visibility Audit — LeadFair"
  description="See how visible your business is on Google, in AI search results, and across the web. Free visibility audit tool coming soon."
>
  <section class="relative overflow-hidden min-h-[80vh] flex items-center bg-dark text-white">
    <div class="absolute inset-0 bg-grid-dark opacity-30"></div>
    <div class="blob blob-green w-96 h-96 -top-32 -left-32 opacity-20"></div>
    <div class="blob blob-blue w-64 h-64 bottom-0 right-10 opacity-15"></div>
    <div class="blob blob-purple w-48 h-48 top-1/3 left-1/4 opacity-10"></div>

    <div class="relative max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center py-20">
      <div class="icon-box icon-box-green w-20 h-20 rounded-2xl mx-auto mb-10 animate-float">
        <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>
      </div>

      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold leading-tight mb-6">
        Free Visibility<br/>
        <span class="gradient-text-green-blue">Audit</span>
      </h1>
      <p class="text-lg sm:text-xl text-gray-300 max-w-xl mx-auto mb-6 leading-relaxed">
        We're building a tool that checks how visible your business is on Google, in AI-powered search results, and across the web — and tells you exactly what to fix.
      </p>

      <div class="inline-flex items-center gap-2 bg-secondary-500/20 text-secondary-300 px-5 py-2 rounded-full text-sm font-semibold mb-10">
        <div class="w-2 h-2 bg-secondary-400 rounded-full animate-pulse"></div>
        Coming Soon
      </div>

      <!-- Email Capture -->
      <form
        name="audit-notify"
        method="POST"
        data-netlify="true"
        netlify-honeypot="bot-field"
        class="max-w-md mx-auto"
      >
        <input type="hidden" name="form-name" value="audit-notify" />
        <p class="hidden"><label>Don't fill this out: <input name="bot-field" /></label></p>
        <p class="text-gray-400 text-sm mb-4">Get notified when it launches:</p>
        <div class="flex gap-3">
          <input
            type="email"
            name="email"
            required
            placeholder="your@email.com"
            class="flex-1 px-5 py-3.5 rounded-xl bg-dark-light border border-gray-700 text-white placeholder-gray-500 focus:border-secondary-500 focus:ring-2 focus:ring-secondary-900 outline-none transition-all text-base"
          />
          <button
            type="submit"
            class="bg-accent-500 hover:bg-accent-600 text-white px-6 py-3.5 rounded-xl font-semibold transition-all btn-scale shadow-glow-orange whitespace-nowrap"
          >
            Notify Me
          </button>
        </div>
      </form>

      <p class="mt-10 text-gray-500 text-sm">
        Can't wait? <a href="/contact" class="text-secondary-400 hover:text-secondary-300 font-medium underline underline-offset-2">Book a call</a> and we'll audit your visibility together.
      </p>
    </div>
  </section>
</BaseLayout>
