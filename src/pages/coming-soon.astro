---
// Coming Soon — standalone page matching main site aesthetic
---

<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>LeadFair — AI That Runs Your Business</title>
  <meta name="description" content="LeadFair is an AI-powered business platform for small businesses. One AI that handles your calls, leads, scheduling, and more — so you can stop juggling apps and start running your business. Join the waitlist." />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <style>
    /* ===== THEME (matches global.css) ===== */
    :root {
      --primary-50: hsl(217 91% 95%);
      --primary-100: hsl(217 91% 88%);
      --primary-200: hsl(217 91% 78%);
      --primary-300: hsl(217 91% 65%);
      --primary-400: hsl(217 91% 55%);
      --primary-500: hsl(217 91% 45%);
      --primary-600: hsl(217 91% 38%);
      --primary-700: hsl(217 91% 30%);
      --secondary-50: hsl(152 68% 95%);
      --secondary-100: hsl(152 68% 85%);
      --secondary-300: hsl(152 68% 58%);
      --secondary-400: hsl(152 68% 50%);
      --secondary-500: hsl(152 68% 42%);
      --accent-50: hsl(24 95% 95%);
      --accent-100: hsl(24 95% 88%);
      --accent-500: hsl(24 95% 53%);
      --accent-600: hsl(24 95% 45%);
      --dark: hsl(220 20% 8%);
      --dark-light: hsl(220 15% 12%);
      --text: hsl(220 15% 18%);
      --text-light: hsl(220 10% 45%);
      --text-lighter: hsl(220 8% 60%);
    }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      background: white;
      color: var(--text);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      overflow-x: hidden;
    }

    /* ===== GRADIENT TEXT (from global.css) ===== */
    .gradient-text {
      background: linear-gradient(135deg, hsl(217 91% 55%), hsl(152 68% 48%), hsl(24 95% 53%));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    /* ===== BLOBS (from global.css) ===== */
    .blob {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      opacity: 0.5;
      pointer-events: none;
    }
    .blob-blue { background: hsl(217 91% 70%); }
    .blob-green { background: hsl(152 68% 65%); }
    .blob-orange { background: hsl(24 95% 70%); }

    /* ===== GRID PATTERN ===== */
    .bg-grid {
      background-image:
        linear-gradient(hsl(220 15% 90% / 0.5) 1px, transparent 1px),
        linear-gradient(90deg, hsl(220 15% 90% / 0.5) 1px, transparent 1px);
      background-size: 60px 60px;
    }

    .bg-dots {
      background-image: radial-gradient(hsl(220 15% 85%) 1px, transparent 1px);
      background-size: 24px 24px;
    }

    /* ===== FLOAT ANIMATION (from global.css) ===== */
    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }
    .animate-float { animation: float 6s ease-in-out infinite; }
    .animate-float-delay { animation: float 6s ease-in-out infinite; animation-delay: 2s; }
    .animate-float-slow { animation: float 8s ease-in-out infinite; animation-delay: 4s; }

    /* ===== SHIMMER LINE ===== */
    .shimmer-line {
      width: 100%;
      height: 1px;
      background: linear-gradient(90deg, transparent, hsl(217 91% 65%), hsl(152 68% 55%), hsl(24 95% 60%), transparent);
      opacity: 0.6;
    }

    /* ===== ICON BOX (from global.css) ===== */
    .icon-box {
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 1rem;
    }
    .icon-box-blue {
      background: linear-gradient(135deg, hsl(217 91% 45%), hsl(217 91% 55%));
      box-shadow: 0 8px 24px hsl(217 91% 45% / 0.25);
    }
    .icon-box-green {
      background: linear-gradient(135deg, hsl(152 68% 42%), hsl(152 68% 52%));
      box-shadow: 0 8px 24px hsl(152 68% 42% / 0.25);
    }
    .icon-box-orange {
      background: linear-gradient(135deg, hsl(24 95% 50%), hsl(24 95% 60%));
      box-shadow: 0 8px 24px hsl(24 95% 53% / 0.25);
    }

    /* ===== CARD HOVER (from global.css) ===== */
    .card-hover {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .card-hover:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
    }

    /* ===== BUTTON EFFECTS ===== */
    .btn-scale {
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .btn-scale:hover { transform: scale(1.03); }

    /* ===== SCROLL REVEAL ===== */
    .reveal {
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
                  transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
    }
    .reveal.revealed {
      opacity: 1;
      transform: translateY(0);
    }
    .reveal-stagger > .reveal {
      transition-delay: calc(var(--reveal-index, 0) * 100ms);
    }
    @media (prefers-reduced-motion: reduce) {
      .reveal { opacity: 1; transform: none; transition: none; }
    }

    /* ===== HERO ===== */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      overflow: hidden;
      background: linear-gradient(180deg, hsl(217 91% 97%) 0%, white 100%);
      padding: 2rem 1rem;
    }

    .hero-inner {
      position: relative;
      z-index: 10;
      max-width: 64rem;
      margin: 0 auto;
      padding: 0 1rem;
      text-align: center;
      width: 100%;
    }

    .logo {
      font-weight: 800;
      font-size: 1.75rem;
      letter-spacing: -0.02em;
      background: linear-gradient(135deg, hsl(217 91% 55%), hsl(152 68% 48%), hsl(24 95% 53%));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 2rem;
    }

    .badge {
      display: inline-block;
      background: var(--primary-100);
      color: var(--primary-700);
      padding: 0.375rem 1rem;
      border-radius: 9999px;
      font-size: 0.875rem;
      font-weight: 600;
      margin-bottom: 2rem;
    }

    h1 {
      font-size: 3rem;
      font-weight: 800;
      line-height: 1.1;
      letter-spacing: -0.025em;
      color: var(--text);
      margin-bottom: 1.5rem;
    }
    @media (min-width: 640px) { h1 { font-size: 3.75rem; } }
    @media (min-width: 1024px) { h1 { font-size: 4.5rem; } }

    .subtitle {
      color: var(--text-light);
      font-size: 1.25rem;
      line-height: 1.7;
      max-width: 48rem;
      margin: 0 auto 2.5rem;
    }
    @media (min-width: 768px) { .subtitle { font-size: 1.375rem; } }

    /* ===== FORM ===== */
    .form-section {
      max-width: 480px;
      margin: 0 auto 2.5rem;
    }

    .form-row {
      display: flex;
      gap: 0.75rem;
    }

    .form-row input {
      flex: 1;
      padding: 1rem 1.25rem;
      background: white;
      border: 1.5px solid hsl(220 15% 88%);
      border-radius: 0.75rem;
      color: var(--text);
      font-family: inherit;
      font-size: 1rem;
      outline: none;
      transition: border-color 0.2s, box-shadow 0.2s;
    }
    .form-row input::placeholder { color: var(--text-lighter); }
    .form-row input:focus {
      border-color: var(--primary-400);
      box-shadow: 0 0 0 3px hsla(217 91% 55% / 0.12);
    }

    .cta-btn {
      padding: 1rem 2rem;
      background: var(--accent-500);
      color: white;
      font-family: inherit;
      font-size: 1rem;
      font-weight: 600;
      border: none;
      border-radius: 0.75rem;
      cursor: pointer;
      white-space: nowrap;
      box-shadow: 0 4px 14px hsl(24 95% 53% / 0.4);
    }
    .cta-btn:hover { background: var(--accent-600); transform: scale(1.03); }

    .form-fine-print {
      color: var(--text-lighter);
      font-size: 0.8rem;
      margin-top: 0.75rem;
      text-align: center;
    }

    .success-msg {
      display: none;
      text-align: center;
      padding: 1.5rem;
      background: var(--secondary-50);
      border: 1px solid var(--secondary-100);
      border-radius: 0.75rem;
    }
    .success-msg h3 {
      font-size: 1.125rem;
      font-weight: 700;
      margin-bottom: 0.25rem;
      color: var(--text);
    }
    .success-msg p {
      color: var(--text-light);
      font-size: 0.95rem;
    }

    /* ===== TRUST DOTS ===== */
    .trust-dots {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 2rem;
      font-size: 0.875rem;
      color: var(--text-lighter);
    }
    .trust-dots span {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .trust-dots .dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--secondary-400);
    }

    /* ===== FEATURES SECTION ===== */
    .features-section {
      padding: 6rem 1rem;
      position: relative;
      overflow: hidden;
      background: linear-gradient(135deg, hsl(217 91% 94%) 0%, hsl(180 40% 94%) 50%, hsl(152 68% 94%) 100%);
    }

    .section-badge {
      display: inline-block;
      background: var(--primary-100);
      color: var(--primary-700);
      padding: 0.375rem 1rem;
      border-radius: 9999px;
      font-size: 0.875rem;
      font-weight: 600;
    }

    .section-header {
      text-align: center;
      max-width: 48rem;
      margin: 0 auto 4rem;
    }
    .section-header h2 {
      font-size: 2.25rem;
      font-weight: 700;
      margin-top: 1rem;
      margin-bottom: 1rem;
    }
    @media (min-width: 768px) { .section-header h2 { font-size: 3rem; } }
    .section-header p {
      color: var(--text-light);
      font-size: 1.125rem;
      line-height: 1.7;
    }

    .features-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1.5rem;
      max-width: 72rem;
      margin: 0 auto;
    }
    @media (min-width: 640px) { .features-grid { grid-template-columns: 1fr 1fr; } }
    @media (min-width: 1024px) { .features-grid { grid-template-columns: 1fr 1fr 1fr; } }

    .feature-card {
      background: white;
      border: 1px solid hsl(220 15% 92%);
      border-radius: 1rem;
      padding: 1.5rem;
      display: flex;
      align-items: flex-start;
      gap: 1.25rem;
    }
    .feature-card .icon-wrap {
      width: 48px;
      height: 48px;
      border-radius: 0.75rem;
      flex-shrink: 0;
    }
    .feature-card .icon-wrap svg {
      width: 24px;
      height: 24px;
      color: white;
    }
    .feature-card h3 {
      font-size: 1rem;
      font-weight: 700;
      margin-bottom: 0.25rem;
    }
    .feature-card p {
      font-size: 0.9rem;
      color: var(--text-light);
      line-height: 1.5;
    }

    /* ===== DASHBOARD PREVIEW ===== */
    .preview-section {
      padding: 6rem 1rem;
      text-align: center;
      position: relative;
      overflow: hidden;
      background: white;
    }

    .dashboard-mock {
      max-width: 56rem;
      margin: 0 auto;
      background: var(--dark);
      border-radius: 1rem;
      overflow: hidden;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    }
    .mock-toolbar {
      display: flex;
      align-items: center;
      gap: 0.375rem;
      padding: 0.75rem 1rem;
      background: var(--dark);
      border-bottom: 1px solid rgba(255,255,255,0.06);
    }
    .mock-dot { width: 10px; height: 10px; border-radius: 50%; }
    .mock-dot-r { background: #ef4444; }
    .mock-dot-y { background: #eab308; }
    .mock-dot-g { background: #22c55e; }
    .mock-toolbar span {
      margin-left: auto;
      font-size: 0.75rem;
      color: hsl(220 8% 55%);
      font-family: monospace;
    }
    .mock-body {
      display: grid;
      grid-template-columns: 170px 1fr;
      min-height: 300px;
    }
    @media (max-width: 639px) {
      .mock-body { grid-template-columns: 1fr; }
      .mock-sidebar { display: none; }
    }
    .mock-sidebar {
      background: rgba(255,255,255,0.02);
      border-right: 1px solid rgba(255,255,255,0.06);
      padding: 1.25rem 0.75rem;
    }
    .mock-nav-item {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.625rem;
      border-radius: 0.375rem;
      font-size: 0.75rem;
      color: hsl(220 8% 55%);
      margin-bottom: 0.25rem;
    }
    .mock-nav-item.active {
      background: hsla(217 91% 55% / 0.15);
      color: hsl(217 91% 78%);
    }
    .mock-nav-item svg { width: 14px; height: 14px; flex-shrink: 0; }
    .mock-main { padding: 1.5rem; }
    .mock-greeting { font-size: 1.05rem; font-weight: 600; color: hsl(220 15% 90%); margin-bottom: 0.25rem; }
    .mock-subgreeting { font-size: 0.75rem; color: hsl(220 8% 55%); margin-bottom: 1.25rem; }
    .mock-cards { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 0.75rem; margin-bottom: 1.25rem; }
    @media (max-width: 500px) { .mock-cards { grid-template-columns: 1fr 1fr; } }
    .mock-stat {
      background: rgba(255,255,255,0.03);
      border: 1px solid rgba(255,255,255,0.06);
      border-radius: 0.5rem;
      padding: 0.875rem;
    }
    .mock-stat .label { font-size: 0.65rem; color: hsl(220 8% 55%); text-transform: uppercase; letter-spacing: 0.04em; margin-bottom: 0.375rem; }
    .mock-stat .value { font-size: 1.375rem; font-weight: 700; color: hsl(220 15% 90%); }
    .mock-stat .change { font-size: 0.65rem; color: hsl(152 68% 50%); margin-top: 0.25rem; }
    .mock-companion {
      background: hsla(217 91% 55% / 0.08);
      border: 1px solid hsla(217 91% 55% / 0.15);
      border-radius: 0.5rem;
      padding: 0.875rem;
    }
    .mock-companion .comp-label { font-size: 0.65rem; font-weight: 600; color: hsl(217 91% 78%); text-transform: uppercase; letter-spacing: 0.04em; margin-bottom: 0.5rem; }
    .mock-companion .comp-msg { font-size: 0.8rem; color: hsl(220 15% 82%); line-height: 1.5; }

    /* ===== BOTTOM CTA ===== */
    .bottom-cta {
      text-align: center;
      padding: 6rem 1rem;
      background: var(--dark);
      color: white;
      position: relative;
      overflow: hidden;
    }
    .bottom-cta h2 {
      font-size: 2.25rem;
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 1rem;
    }
    @media (min-width: 768px) { .bottom-cta h2 { font-size: 3rem; } }
    .bottom-cta > p {
      color: hsl(220 8% 55%);
      font-size: 1.125rem;
      line-height: 1.7;
      max-width: 32rem;
      margin: 0 auto 2.5rem;
    }

    .bottom-form { max-width: 420px; margin: 0 auto; }
    .bottom-form .form-row input {
      background: rgba(255,255,255,0.05);
      border-color: rgba(255,255,255,0.1);
      color: white;
    }
    .bottom-form .form-row input::placeholder { color: hsl(220 8% 50%); }
    .bottom-form .form-row input:focus {
      border-color: var(--primary-400);
      box-shadow: 0 0 0 3px hsla(217 91% 55% / 0.2);
    }

    .bg-grid-dark {
      background-image:
        linear-gradient(hsl(220 15% 20% / 0.3) 1px, transparent 1px),
        linear-gradient(90deg, hsl(220 15% 20% / 0.3) 1px, transparent 1px);
      background-size: 60px 60px;
    }

    /* ===== FOOTER ===== */
    .footer {
      text-align: center;
      padding: 1.5rem;
      color: hsl(220 8% 45%);
      font-size: 0.8rem;
      background: var(--dark);
      border-top: 1px solid rgba(255,255,255,0.06);
    }

    /* ===== BUILT-BY LINE ===== */
    .built-by {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      color: var(--text-lighter);
      font-size: 0.85rem;
      margin-top: 3rem;
    }
    .built-by svg { width: 16px; height: 16px; flex-shrink: 0; color: var(--text-lighter); }
  </style>
</head>
<body>

  <!-- ===== HERO ===== -->
  <section class="hero">
    <!-- Decorative blobs -->
    <div class="blob blob-blue animate-float" style="width:384px;height:384px;top:-80px;left:-80px;"></div>
    <div class="blob blob-green animate-float-delay" style="width:320px;height:320px;top:33%;right:-80px;"></div>
    <div class="blob blob-orange animate-float-slow" style="width:256px;height:256px;bottom:40px;left:25%;opacity:0.3;"></div>
    <!-- Grid overlay -->
    <div style="position:absolute;inset:0;opacity:0.4;" class="bg-grid"></div>

    <div class="hero-inner">
      <div class="logo">LeadFair</div>
      <div class="badge">Launching Soon</div>

      <h1>
        Stop Juggling Apps.<br/>
        <span class="gradient-text">Start Running Your Business.</span>
      </h1>

      <p class="subtitle">One platform that answers your calls, manages your leads, automates your follow-ups, and gets you found online — all powered by an AI that knows your business inside and out.</p>

      <div class="form-section">
        <form id="waitlist-form" name="waitlist" method="POST" data-netlify="true" netlify-honeypot="bot-field">
          <input type="hidden" name="form-name" value="waitlist" />
          <p style="display:none"><input name="bot-field" /></p>
          <div class="form-row">
            <input type="email" name="email" placeholder="Your email address" required />
            <button type="submit" class="cta-btn btn-scale">Join Waitlist</button>
          </div>
        </form>
        <div class="success-msg" id="success-msg">
          <h3>You're on the list!</h3>
          <p>We'll send one email when LeadFair is ready. That's it.</p>
        </div>
        <p class="form-fine-print">No spam. One email when we launch.</p>
      </div>

      <div class="trust-dots">
        <span><span class="dot"></span>Set up in one day</span>
        <span><span class="dot"></span>Cancel anytime</span>
        <span><span class="dot"></span>Everything included — no add-ons</span>
      </div>
    </div>
  </section>

  <div class="shimmer-line"></div>

  <!-- ===== FEATURES ===== -->
  <section class="features-section">
    <div style="position:absolute;inset:0;opacity:0.3;" class="bg-dots"></div>
    <div style="position:relative;z-index:10;">
      <div class="section-header">
        <div class="section-badge">Everything in One Place</div>
        <h2>Everything Your Business Needs. One Platform.</h2>
        <p>No more switching between five different apps. LeadFair brings it all together — powered by AI that actually gets work done.</p>
      </div>

      <div class="features-grid reveal-stagger">
        <div class="feature-card card-hover reveal" style="--reveal-index: 0;">
          <div class="icon-wrap icon-box icon-box-blue">
            <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" /></svg>
          </div>
          <div>
            <h3>AI Receptionist</h3>
            <p>Answers calls, books appointments, and captures leads 24/7 — in a natural voice.</p>
          </div>
        </div>

        <div class="feature-card card-hover reveal" style="--reveal-index: 1;">
          <div class="icon-wrap icon-box icon-box-green">
            <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" /></svg>
          </div>
          <div>
            <h3>Visibility & SEO</h3>
            <p>Get found online. LeadFair optimizes your presence so customers discover you first.</p>
          </div>
        </div>

        <div class="feature-card card-hover reveal" style="--reveal-index: 2;">
          <div class="icon-wrap icon-box icon-box-orange">
            <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z" /></svg>
          </div>
          <div>
            <h3>CRM & Dashboard</h3>
            <p>Every lead, customer, and conversation in one place. See your whole business at a glance.</p>
          </div>
        </div>

        <div class="feature-card card-hover reveal" style="--reveal-index: 3;">
          <div class="icon-wrap icon-box icon-box-blue">
            <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg>
          </div>
          <div>
            <h3>Automations</h3>
            <p>Follow-ups, reminders, and workflows that run themselves. Just tell the AI what you need.</p>
          </div>
        </div>

        <div class="feature-card card-hover reveal" style="--reveal-index: 4;">
          <div class="icon-wrap icon-box icon-box-green">
            <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" /></svg>
          </div>
          <div>
            <h3>Company Companion</h3>
            <p>An AI that knows your business inside and out. Ask it anything, or let it handle it for you.</p>
          </div>
        </div>

        <div class="feature-card card-hover reveal" style="--reveal-index: 5;">
          <div class="icon-wrap icon-box icon-box-orange">
            <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" /></svg>
          </div>
          <div>
            <h3>AI Security</h3>
            <p>Proprietary monitoring keeps your business data safe from AI-era threats. Always on.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="shimmer-line"></div>

  <!-- ===== DASHBOARD PREVIEW ===== -->
  <section class="preview-section">
    <div style="position:absolute;inset:0;opacity:0.3;" class="bg-grid"></div>
    <div style="position:relative;z-index:10;">
      <div class="section-header">
        <div class="section-badge">The Dashboard</div>
        <h2>Your Business, at a Glance</h2>
        <p>A dashboard that tells you what matters — and an AI companion that helps you act on it.</p>
      </div>

      <div class="dashboard-mock reveal">
        <div class="mock-toolbar">
          <div class="mock-dot mock-dot-r"></div>
          <div class="mock-dot mock-dot-y"></div>
          <div class="mock-dot mock-dot-g"></div>
          <span>app.leadfair.ai</span>
        </div>
        <div class="mock-body">
          <div class="mock-sidebar">
            <div class="mock-nav-item active">
              <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-4 0h4" /></svg>
              Dashboard
            </div>
            <div class="mock-nav-item">
              <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z" /></svg>
              Leads
            </div>
            <div class="mock-nav-item">
              <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" /></svg>
              Schedule
            </div>
            <div class="mock-nav-item">
              <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" /></svg>
              Companion
            </div>
            <div class="mock-nav-item">
              <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" /><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" /></svg>
              Settings
            </div>
          </div>
          <div class="mock-main">
            <div class="mock-greeting">Good morning, Jeremy</div>
            <div class="mock-subgreeting">Here's how your business is doing today</div>
            <div class="mock-cards">
              <div class="mock-stat">
                <div class="label">New Leads</div>
                <div class="value">12</div>
                <div class="change">+3 today</div>
              </div>
              <div class="mock-stat">
                <div class="label">Appointments</div>
                <div class="value">8</div>
                <div class="change">2 upcoming</div>
              </div>
              <div class="mock-stat">
                <div class="label">Revenue</div>
                <div class="value">$4.2k</div>
                <div class="change">+18% this week</div>
              </div>
            </div>
            <div class="mock-companion">
              <div class="comp-label">Companion</div>
              <div class="comp-msg">You have 3 leads that haven't been contacted in 48 hours. Want me to send follow-up texts?</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="shimmer-line"></div>

  <!-- ===== BOTTOM CTA ===== -->
  <section class="bottom-cta">
    <div style="position:absolute;inset:0;opacity:0.3;" class="bg-grid-dark"></div>
    <div style="position:relative;z-index:10;">
      <h2>$199/mo. Everything Included.</h2>
      <p>No hidden fees, no per-feature pricing, no surprises. One plan that covers your entire business.</p>

      <div class="bottom-form">
        <form id="waitlist-form-bottom" name="waitlist" method="POST" data-netlify="true" netlify-honeypot="bot-field">
          <input type="hidden" name="form-name" value="waitlist" />
          <p style="display:none"><input name="bot-field" /></p>
          <div class="form-row">
            <input type="email" name="email" placeholder="Your email address" required />
            <button type="submit" class="cta-btn btn-scale">Join Waitlist</button>
          </div>
        </form>
        <div class="success-msg" id="success-msg-bottom" style="background: rgba(255,255,255,0.05); border-color: rgba(255,255,255,0.1);">
          <h3 style="color: #fff;">You're on the list!</h3>
          <p style="color: hsl(220 8% 55%);">We'll send one email when LeadFair is ready.</p>
        </div>
      </div>

      <div class="built-by" style="color: hsl(220 8% 45%); margin-top: 2.5rem;">
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" /><path stroke-linecap="round" stroke-linejoin="round" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" /></svg>
        Built in Huntsville, AL by a small business owner
      </div>
    </div>
  </section>

  <!-- ===== FOOTER ===== -->
  <footer class="footer">&copy; 2026 LeadFair. Huntsville, Alabama.</footer>

  <script>
    // Form submissions
    document.querySelectorAll('form[name="waitlist"]').forEach(form => {
      const msg = form.nextElementSibling;
      const btn = form.querySelector('.cta-btn');

      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        btn.textContent = 'Joining…';
        btn.disabled = true;

        try {
          await fetch('/', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: new URLSearchParams(new FormData(form)).toString()
          });
          form.style.display = 'none';
          msg.style.display = 'block';
          const finePrint = form.parentElement.querySelector('.form-fine-print');
          if (finePrint) finePrint.style.display = 'none';
        } catch {
          btn.textContent = 'Join Waitlist';
          btn.disabled = false;
        }
      });
    });

    // Scroll reveal
    const reveals = document.querySelectorAll('.reveal');
    if (reveals.length) {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('revealed');
          }
        });
      }, { threshold: 0.1 });
      reveals.forEach(el => observer.observe(el));
    }
  </script>
</body>
</html>
