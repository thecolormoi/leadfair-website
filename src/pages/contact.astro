---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Contact LeadFair — Get in Touch"
  description="Ready to talk about AI tools and visibility for your business? Reach out — no sales pitch, just an honest conversation about what might help."
>
  <!-- Hero -->
  <section class="bg-surface py-16 lg:py-24 border-b border-gray-100">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h1 class="text-4xl sm:text-5xl font-extrabold leading-tight mb-6">Let's Talk</h1>
      <p class="text-lg text-text-light max-w-2xl mx-auto leading-relaxed">No sales pitch. No pressure. Just a conversation about your business and whether we can help.</p>
    </div>
  </section>

  <!-- Contact Form -->
  <section class="py-20 bg-white">
    <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
      <form
        name="contact"
        method="POST"
        data-netlify="true"
        netlify-honeypot="bot-field"
        class="space-y-6"
      >
        <input type="hidden" name="form-name" value="contact" />
        <p class="hidden">
          <label>Don't fill this out: <input name="bot-field" /></label>
        </p>

        <div class="grid sm:grid-cols-2 gap-6">
          <div>
            <label for="name" class="block text-sm font-medium text-text mb-2">Your Name</label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:border-primary-500 focus:ring-2 focus:ring-primary-100 outline-none transition-colors"
              placeholder="Jane Smith"
            />
          </div>
          <div>
            <label for="email" class="block text-sm font-medium text-text mb-2">Email</label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:border-primary-500 focus:ring-2 focus:ring-primary-100 outline-none transition-colors"
              placeholder="jane@example.com"
            />
          </div>
        </div>

        <div>
          <label for="business" class="block text-sm font-medium text-text mb-2">Business Name</label>
          <input
            type="text"
            id="business"
            name="business"
            class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:border-primary-500 focus:ring-2 focus:ring-primary-100 outline-none transition-colors"
            placeholder="Your Business Name"
          />
        </div>

        <div>
          <label for="interest" class="block text-sm font-medium text-text mb-2">What are you interested in?</label>
          <select
            id="interest"
            name="interest"
            class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:border-primary-500 focus:ring-2 focus:ring-primary-100 outline-none transition-colors bg-white"
          >
            <option value="">Select one...</option>
            <option value="ai-receptionist">AI Receptionist</option>
            <option value="company-companion">Company Companion</option>
            <option value="visibility">Visibility (SEO/AEO/GEO)</option>
            <option value="automations">Automations</option>
            <option value="diagnostic">Business Diagnostic</option>
            <option value="not-sure">Not sure yet — just exploring</option>
          </select>
        </div>

        <div>
          <label for="message" class="block text-sm font-medium text-text mb-2">Tell us about your business</label>
          <textarea
            id="message"
            name="message"
            rows="5"
            class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:border-primary-500 focus:ring-2 focus:ring-primary-100 outline-none transition-colors resize-y"
            placeholder="What does your business do? What's your biggest challenge right now?"
          ></textarea>
        </div>

        <button
          type="submit"
          class="w-full bg-primary-500 text-white px-8 py-3 rounded-lg font-semibold hover:bg-primary-600 transition-colors text-lg"
        >
          Send Message
        </button>
      </form>

      <div class="mt-12 pt-8 border-t border-gray-100 text-center">
        <p class="text-text-light mb-2">Based in Huntsville, Alabama</p>
        <p class="text-text-lighter text-sm">We typically respond within one business day.</p>
      </div>
    </div>
  </section>
</BaseLayout>
